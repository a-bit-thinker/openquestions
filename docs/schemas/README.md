# Schemas

These JSON Schema files define the required shape for canonical YAML/JSON artifacts.

Target validation flow:
1. Load YAML/JSON artifact.
2. Validate against matching schema.
3. Fail round close on schema mismatch.
